services:
  cim:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./media:/app/media
      - ./data:/app/data
      - ./logs:/app/logs
      - ./models:/app/models
      - ./app_config.json:/app/app_config.json
    restart: unless-stopped